<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:background="@color/ColorGrayLight"
        android:gravity="center"
        android:text="Paytm OTP"
        android:textStyle="bold"
        android:textColor="#000080"
        android:textSize="18sp" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center"
            android:layout_margin="10dp"
            android:orientation="vertical">

            <android.support.design.widget.TextInputLayout style="@style/inputTextView">

                <EditText
                    android:id="@+id/paytmotpEditText"
                    style="@style/editText"
                    android:drawableLeft="@drawable/paytm_mobile_icon"
                    android:drawablePadding="25dp"
                    android:hint="Enter OTP"
                    android:inputType="number"
                    android:maxLength="6" />
            </android.support.design.widget.TextInputLayout>

            <Button
                android:id="@+id/paytmotpButton"
                android:layout_width="150dp"
                android:layout_height="50dp"
                android:layout_gravity="center"
                android:layout_marginTop="20dp"
                android:text="@string/submit"
                android:textColor="@android:color/white"
                android:textSize="@dimen/contentSizeLarge"
                android:theme="@style/button_style" />

            <TextView
                android:id="@+id/resendOtp"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="end"
                android:layout_marginEnd="10dp"
                android:layout_marginTop="30dp"
                android:text="@string/resend"
                android:textColor="@color/ColorBlack"
                android:textSize="14dp" />
        </LinearLayout>
    </ScrollView>
</LinearLayout>