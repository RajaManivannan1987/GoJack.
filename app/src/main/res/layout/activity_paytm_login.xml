<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:background="@color/ColorGrayLight"
        android:gravity="center"
        android:text="Link your Paytm account"
        android:textColor="#000080"
        android:textStyle="bold"
        android:textSize="18sp" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center"
            android:orientation="vertical"
            android:layout_margin="10dp">

            <android.support.design.widget.TextInputLayout
                android:id="@+id/paytmMobileNoTextInputLayout"
                style="@style/inputTextView">

                <EditText
                    android:id="@+id/paytmMobileNodEditText"
                    style="@style/editText"
                    android:drawableLeft="@drawable/paytm_mobile_icon"
                    android:drawablePadding="25dp"
                    android:hint="Mobile No"
                    android:inputType="phone"
                    android:maxLength="10"
                    android:text="" />
            </android.support.design.widget.TextInputLayout>

            <android.support.design.widget.TextInputLayout
                android:id="@+id/paytmMailIdTextInputLayout"
                style="@style/inputTextView">

                <EditText
                    android:id="@+id/paytmMailIdEditText"
                    style="@style/editText"
                    android:drawableLeft="@drawable/paytm_email_icon"
                    android:drawablePadding="25dp"
                    android:hint="Mail ID"
                    android:inputType="text"
                    android:text="" />
            </android.support.design.widget.TextInputLayout>

            <Button
                android:id="@+id/paytmLoginButton"
                android:layout_width="150dp"
                android:layout_height="50dp"
                android:layout_gravity="center"
                android:layout_marginTop="20dp"
                android:text="@string/submit"
                android:textColor="@android:color/white"
                android:textSize="@dimen/contentSizeLarge"
                android:theme="@style/button_style" />
        </LinearLayout>
    </ScrollView>
</LinearLayout>